[build]
builder = "nixpacks"

[deploy]
startCommand = "streamlit run BergenBookSteamlit.py --server.port $PORT --server.address 0.0.0.0 --server.headless true --server.fileWatcherType none"
healthcheckPath = "/_stcore/health"
restartPolicyType = "on_failure"
